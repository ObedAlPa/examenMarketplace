-- data.sql
-- Datos de ejemplo / seed para el marketplace. Se completará con usuarios de prueba, categorías y productos (con contraseñas hasheadas) en el siguiente paso.

-- Ejemplo: insertar categorías
-- INSERT INTO categorias (id, nombre) VALUES (gen_random_uuid(), 'Electrónica');

-- Ejemplo: usuarios de prueba (hashes se generarán antes de insertar)
-- INSERT INTO usuarios (id, email, password_hash, nombre, rol) VALUES (...);
